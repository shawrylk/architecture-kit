# Glossary

Domain terms, in the domain's own language, with the meaning the code relies on. This file is where
a reader resolves a word they met in a feature name.

Replace every row. The two below are the shape: a term, and what the system does about it.

| Term | Meaning |
|---|---|
| *(domain term)* | *(what it is, and the unit of work it corresponds to)* |
| Tenant | One organization. The unit of data isolation |

Conventions this repository holds to:

- Business timezone and storage timezone are stated here, once.
- User-facing strings are in the product's language; code, comments, logs and API codes are English.
